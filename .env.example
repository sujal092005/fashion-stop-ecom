# Environment Configuration for Fashion Stop E-commerce

# Environment (development/production)
NODE_ENV=production

# Server Configuration
PORT=10000

# MongoDB Configuration
# For Render deployment (automatically provided):
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/fashionstop?retryWrites=true&w=majority
# For local development:
# MONGODB_URI=mongodb://localhost:27017/fashionstop

# Admin Credentials
ADMIN_USERNAME=sujal
ADMIN_PASSWORD=pass123

# Application Settings
APP_NAME=Fashion Stop
APP_VERSION=1.0.0

# Render Deployment Notes:
# - MONGODB_URI will be automatically set by Render from the database service
# - PORT will be automatically set by Render (usually 10000)
# - All other variables are configured in render.yaml
